

let imagen=document.querySelector("tarjeta");

    (function(){
    imagen.addEventListener("click",cambio);
    imagen.addEventListener("mouseout",cambiar);
    }) ();

function cambio () {
objeto.style.transform = 'rotateY(180deg)';
}
function cambiar (){
objeto.style.transform = 'rotateY(180deg)';
}